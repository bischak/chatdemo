# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET         /channel/get/:name         controllers.ChatChannel.get(name:String)
POST        /channel/send/:name        controllers.ChatChannel.send(name:String)
